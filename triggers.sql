/* Mark trigger:
    for any tuple inserted into the table *marks*,
    insert it into the corresponding operation table as well
*/
CREATE FUNCTION mark2operation()
RETURNS TRIGGER AS $$
BEGIN
    INSERT

CREATE OR REPLACE TRIGGER mark2operation
    AFTER INSERT



/* Buy procedure
    1. Insert tuples into *marks*
    2. Insert tuples